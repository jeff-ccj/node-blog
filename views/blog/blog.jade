//-
  * @page    博客列表
  * @author  Jeff Tsui
  * @date    16.04.20
  * @mail    jeff.ccjie@gmail.com

#blog.blog.row
  -if(blogs && blogs.length > 0)
    -each blog in blogs
      .blogList.col-xs-12.col-sm-6(id=blog._id)
        .media
          -if(blog.thumbPic)
            .media-left
              a(href='#')
                img.media-object(src=blog.thumbPic, alt='图片')
          .media-body
            h4.media-heading
              a.blogTitle(href='/blog/'+ blog._id) #{blog.title}

            div #{blog.time.minute}
            div
              span 作者:
              a(href='/user/' + blog.name) #{blog.name}
  -else
    h4 暂无数据
include ../base/paging
  //.blogList.col-xs-12.col-sm-6
  //  .media
  //    .media-left
  //      a(href='#')
  //        img.media-object(src='...', alt='图片')
  //    .media-body
  //      h4.media-heading XXX说
  //      div  内容...
